import { sum } from 'lodash-es'

console.log('sum', sum([1, 2, 3, 4]))

// dep
{
  subs: [Watcher]
}

// Watcher
{
  dep: [dep]
}

this.a = 1
this.b = 2
nextTick()

a: {
  subs: [computedWatcher, Watcher]
}